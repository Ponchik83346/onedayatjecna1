package command;

import model.Player;

public class OpenInventoryCommand implements Command {
    public OpenInventoryCommand(Player player) {
    }

    @Override
    public void execute() {
        
    }
}
